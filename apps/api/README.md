# apps/api

Scaffold placeholder — not yet implemented. Node.js 22 + Express REST API. Wait for the relevant spec's `design.md`/`tasks.md` before building real routes/services here — start with `.claude/specs/identity-and-rbac` (foundation for every other route's authorization).
